code: my-custom-bmad
name: "Axonify Dev-Custom-BMad: Custom Agents, Workflows, and Tasks"
default_selected: true

# Custom workflows
workflows:
  - name: orchestrate-story
    path: workflows/orchestrate-story
    description: "Execute complete BMAD story workflow: create → develop → review → merge"

# Custom tasks
tasks:
  - name: retry-with-feedback
    path: tasks/retry-with-feedback.md
    description: "Execute action with validation and automatic retry on failure"
  - name: validate-workflow
    path: tasks/validate-workflow.xml
    description: "Run a checklist against a document with thorough analysis and produce a validation report"
